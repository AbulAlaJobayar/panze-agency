---
import { Icon } from "../ui/icon";
import * as data from "@/content/career/facilities-content.md";

const { title, subtitle, description, benefits } = data.frontmatter;
---

<section class="">
  <div class="mx-auto">
    <!-- Header -->
    <div
      class="grid
             px-8 py-12
             lg:grid-cols-[180px_1fr_420px]
             lg:px-10"
    >
      <div>
        <span
          class="flex items-center gap-2 font-dm-mono font-normal text-[14px] leading-[150%] tracking-[-4%] text-primary"
        >
          <Icon name="star" class="size-4" />
          {title}
        </span>
      </div>

      <h2
        class="mt-6 lg:mt-0
               max-w-100
               font-commissioner
               font-semibold
               text-[24px]
               leading-[110%] tracking-[-4%] uppercase"
      >
        {subtitle}
      </h2>

      <p
        class="mt-6 lg:mt-0
               justify-self-end
              font-dm-mono font-normal text-[14px] leading-[150%] tracking-[-4%]"
      >
        {description}
      </p>
    </div>

    <!-- Benefits -->
    <div class="grid lg:grid-cols-2 px-10">
      {
        benefits.map((item:any, index:any) => (
          <div
            class:list={[
              "border-b border-black/20",
              index % 2 === 0 && "lg:border-r border-black/20",
            ]}
          >
            <div
              class="grid gap-2
                     px-8 py-10
                     lg:grid-cols-[170px_1fr]
                    "
            >
              <div>
                <Icon name={item.icon} class="mb-2 size-5 text-primary" />

                <h3 class="font-dm-mono font-medium text-[16px] leading-[150%] tracking-[-4%] text-primary">
                  {item.title}
                </h3>
              </div>

              <p class="font-dm-mono font-normal text-[14px] leading-[150%] tracking-[-4%]">
                {item.description}
              </p>
            </div>
          </div>
        ))
      }
    </div>
  </div>
</section>
